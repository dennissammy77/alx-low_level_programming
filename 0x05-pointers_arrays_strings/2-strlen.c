#include "main.h"

/**
 * _strlen - returns length of a string;
 * @s - pointer.
 * Return: lentgh of a string.
 */
int _strlen(char *s)
{
	int length = 0;

	while (*s != '\0')
	{
		s++;
		l++;
	}
	return (l);
}
