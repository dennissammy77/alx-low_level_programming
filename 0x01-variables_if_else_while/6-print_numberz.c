#include <stdio.h>

/**
 * main -Entry point
 *
 * description - prints single digit numbers of bae 10 starting from 0
 *
 * Return: Always(0) success
 *
 * */

int main(void)
{
	int n;

	for (n=0;n<= 9; n++)
		{		putchar(n);
				putchar('\n');
		}
	return(0);
}
