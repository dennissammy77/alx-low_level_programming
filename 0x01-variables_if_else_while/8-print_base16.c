#include <stdio.h>

/**
 * main - Entry point
 *
 * description - prints all numbers of base 16 in lowercase
 *
 * Return: Always(0) success
 *
 * */

int main(void)
{
	int x,n,a,f;

	for (n =0; n<=9; n++)
	{
		putchar(n);
	}
	for (x = a; x<=f; x++)
	{	putchar(x);
		putchar('\n');
	}
	return(0);
}
