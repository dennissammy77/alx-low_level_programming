#include "main.h"
#include <stdio.h>

/**
 * main - Entry point.
 *
 * Description - adds two integers and returns the result.
 */
int add(int n,int x)
{
	int result;

	result = add(n, x);
	_putchar(result);
	return (0);
}
