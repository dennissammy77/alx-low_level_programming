#include "main.h"
#include <stdio.h>

/**
 * main - Entry point.
 *
 * Description - adds two integers and returns the result.
 */
int add(int n,int x)
{
	int result;

	result = n + x;

	return (result);
}
