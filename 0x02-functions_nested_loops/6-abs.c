#include "main.h"
#include <stdio.h>

/**
 * main - Entry point.
 *
 * description - computes absolutes value of an integer.
 *
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
